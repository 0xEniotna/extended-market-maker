# Iter Decision Doc — TSLA_24_5-USD iter001

**Date launched**: 2026-05-16 (off-hours launch — TradFi reopens Sun 22:00 UTC)
**Author**: Antoine
**Iter file**: `/root/MM/.env.tsla_24_5.iter001`
**Baseline**: none (first launch of this market)
**Run ID** (filled at launch): TBD
**Parent screen**: `/root/MM/data/mm_market_screen.txt` 2026-05-16 (score 16.82)

---

## 1. Hypothesis (one sentence, falsifiable)

TSLA_24_5-USD with `MM_SPREAD_MULTIPLIER=0.7` and a $2,000 position cap is profitable enough to justify continued operation over a ≥3-week test window, measured as positive cumulative trade PnL after fees and slippage.

---

## 2. Config diff vs baseline

No baseline — fresh market launch. Configuration modelled on `.env.hood_24_5.iter001`.

| Knob | This iter |
|---|---|
| `MM_MARKET_NAME` | `TSLA_24_5-USD` |
| `MM_SPREAD_MULTIPLIER` | `0.7` |
| `MM_MIN_OFFSET_BPS` | `7` |
| `MM_MAX_OFFSET_BPS` | `60` |
| `MM_NUM_PRICE_LEVELS` | `2` |
| `MM_ORDER_SIZE_MULTIPLIER` | `7` (→ base 0.7 TSLA = $293/order at mid $418) |
| `MM_MAX_ORDER_NOTIONAL_USD` | `300` |
| `MM_MAX_POSITION_NOTIONAL_USD` | `2000` |
| `MM_MAX_POSITION_SIZE` | `10` |
| `MM_DRAWDOWN_STOP_PCT_OF_MAX_NOTIONAL` | `7.5` |
| `MM_DEADMAN_ENABLED` | `false` |

All Stage 1/Stage 2 overlays **default-OFF**. `book_change` instrumented.

---

## 3. Pre-registered success criteria

Same structure as GOOG iter001:

1. **Primary — cumulative PnL** ≥ $0 after fees over ≥3 weeks or ≥100 trades.
2. **Secondary — fill rate** ≥ 30/week during NASDAQ-active periods.
3. **Secondary — mean +5s markout** ≥ −2 bps.
4. **Safety** — no drawdown_stop, no repeated circuit_breaker, latency p95 ≤ 1.5×.

---

## 4. Sample size required

≥ 3 weeks OR ≥ 100 fills. TSLA has higher daily vol ($1.76M) than typical, so expect 40-60 fills/week if our spread band rule is right.

---

## 5. Rollback trigger

Same as GOOG iter001:
- `drawdown_stop` fires ($150 abs)
- Cumul realized PnL < −$200
- Sustained one-sided bias > 24h active hours
- Zero active-hours fills in 48h

Rollback:
```bash
ssh mm-bot 'cd /root/MM && PATH=/root/MM/.venv/bin:$PATH mmctl stop tsla_24_5.iter001'
```

---

## 6. Comparison baseline

No baseline (fresh launch). Compare against HOOD/ORCL/MU patterns once those have data.

TSLA-specific: single-stock derivative, higher beta than indices, **higher event risk** (Elon news cycles, EV-sector swings).

---

## 7. Post-mortem (filled after test ends)

(same template as GOOG iter001)

### Decision

**[KEEP / ROLLBACK / EXTEND / INCONCLUSIVE]**

---

## 8. Links

- Screen output: `/root/MM/data/mm_market_screen.txt`
- Template config: `/root/MM/.env.hood_24_5.iter001`
- Journals: `/root/MM/data/mm_journal/mm_TSLA_24_5-USD_*.jsonl`

---

## Known risks

1. **TSLA event volatility.** Single-stock with very high news beta. Earnings, Elon Twitter, EV market moves all affect prices significantly. Caps and drawdown thresholds should hold.
2. **Off-hours launch caveat** (same as GOOG).
3. **Position size assumptions.** At mid $418 with MAX_POSITION_NOTIONAL_USD=$2000, max position is ~4.8 TSLA. If TSLA gaps 5% on news, that's $100 unrealized swing on full inventory — manageable.
