# Iter Decision Doc — TECH100m-USD iter001

**Date prepared**: 2026-05-16
**Target launch**: Sun 2026-05-17 22:00 UTC (alongside HOOD/ORCL, when NASDAQ reopens)
**Author**: Antoine
**Iter file**: `/root/MM/.env.tech100m.iter001`
**Baseline**: none (first launch of this market)
**Run ID** (filled at launch): TBD
**Parent screen**: `/root/MM/data/mm_market_screen.txt` 2026-05-16 (score 16.4)

---

## 1. Hypothesis (one sentence, falsifiable)

TECH100m-USD with `MM_SPREAD_MULTIPLIER=0.7` and a $2,000 position cap is profitable enough to justify continued operation over a ≥3-week test window, measured as positive cumulative trade PnL after fees and slippage.

---

## 2. Config diff vs baseline

No baseline — fresh launch. Configuration modelled on `.env.hood_24_5.iter001` with TECH100m-specific sizing.

| Knob | This iter |
|---|---|
| `MM_MARKET_NAME` | `TECH100m-USD` |
| `MM_SPREAD_MULTIPLIER` | `0.7` |
| `MM_MIN_OFFSET_BPS` | `7` |
| `MM_MAX_OFFSET_BPS` | `60` |
| `MM_NUM_PRICE_LEVELS` | `2` |
| `MM_ORDER_SIZE_MULTIPLIER` | `7` (→ base 0.007 units = $203/order at mid $28,949) |
| `MM_MAX_ORDER_NOTIONAL_USD` | `300` |
| `MM_MAX_POSITION_NOTIONAL_USD` | `2000` |
| `MM_MAX_POSITION_SIZE` | `0.5` (~$14k headroom on $28k mid) |
| `MM_DRAWDOWN_STOP_PCT_OF_MAX_NOTIONAL` | `7.5` |
| `MM_DEADMAN_ENABLED` | `false` |

All Stage 1/Stage 2 overlays **default-OFF**. `book_change` instrumented.

---

## 3. Pre-registered success criteria

Same structure as GOOG iter001. Adjustments for TECH100m:

1. **Primary — cumulative PnL** ≥ $0 after fees over ≥3 weeks or ≥100 trades.
2. **Secondary — fill rate** ≥ 30/week during NASDAQ-active periods.
3. **Secondary — mean +5s markout** ≥ −2 bps.
4. **Safety** — no drawdown_stop, no repeated circuit_breaker, latency p95 ≤ 1.5×.

Special note: low daily volume ($211K) is a concern. May see fewer fills than GOOG/TSLA. Watch for ≥1 fill per active hour minimum.

---

## 4. Sample size required

≥ 3 weeks OR ≥ 100 fills. Given low daily vol, may need to extend test window.

---

## 5. Rollback trigger

Same as GOOG iter001. Additionally:
- **Low volume / no fills:** If <5 fills in first week of active hours, the market is too thin for our size — pause and re-evaluate ORDER_SIZE_MULTIPLIER or step down to smaller minimum.

Rollback:
```bash
ssh mm-bot 'cd /root/MM && PATH=/root/MM/.venv/bin:$PATH mmctl stop tech100m.iter001'
```

---

## 6. Comparison baseline

No baseline. Compare against TradFi index references (SPX500m was killed for adverse selection at 5 bps spread — TECH100m has a wider 20 bps spread which should be safer per the spread-band rule).

---

## 7. Launch decision — Sunday 22:00 UTC

Defer launch until NASDAQ reopens. Trading hours for TECH100m are likely tied to NASDAQ (NDX index follows tech). Off-hours launch may produce 0 fills and confound diagnostics.

If by Sunday evening this assumption proves wrong (TECH100m turns out to be 24/7), can launch immediately.

---

## 8. Post-mortem (filled after test ends)

(same template as GOOG iter001)

### Decision

**[KEEP / ROLLBACK / EXTEND / INCONCLUSIVE]**

---

## 9. Links

- Screen output: `/root/MM/data/mm_market_screen.txt`
- Template config: `/root/MM/.env.hood_24_5.iter001`
- Journals (after launch): `/root/MM/data/mm_journal/mm_TECH100m-USD_*.jsonl`

---

## Known risks

1. **Low daily volume ($211K).** Limits realistic fill rate. May need wider MIN_OFFSET to compensate.
2. **High mid price ($28,949).** Single contract = $29 worth — fine for sizing math, but minimum-size 0.001 means we can't easily go smaller if our size proves too aggressive.
3. **Index basket vs single name.** NASDAQ-100 index reflects 100 names; idiosyncratic risk is dampened vs single-stock TSLA/GOOG. Should be steadier.
4. **Trading hours uncertainty.** Treating as 24/5 by default; will revisit if behavior suggests otherwise.
